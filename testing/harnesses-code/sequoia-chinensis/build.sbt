scalaVersion := "2.12.4"

name := "sequoia-chinensis-harness"
organization:= "org.davala"
version := "0.1.0"

mainClass in (Compile,run) := Some("sequoiaaRun")

//version := "0.1"
//mainClass in (Compile, packageBin) := Some("org.davala.owldtelim.AOWLDatatypeEliminator"),
//mainClass in (Compile, run):= Some("org.davala.owldtelim.OWLDatatypeEliminator")


	
	
